.claude
plans/

# Rust
target/
Cargo.lock

# Matrix/bench harness build products (tools/matrixtest, tools/bench).
out/

# Dev heap-preload dylib install prefix (crates/sismo/build.rs).
dev-install/

# Hermetic build deps + source clones installed by tools/install-build-deps.
third_party/bin/
third_party/src/
third_party/perfmon/

# Tool-specific Python virtualenvs (e.g. tools/e2e-all-sources bootstraps
# one with the `perfetto` package).
third_party/venv/

# Python
__pycache__/
*.pyc

# Perfetto
*.pftrace

# clangd / IDE compile database.
compile_commands.json

# Local working docs / design notes — not committed.
working-docs/
.claude/settings.local.json
